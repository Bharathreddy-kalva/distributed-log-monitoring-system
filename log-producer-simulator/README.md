# Log Producer Simulator

A Python-based simulator that generates synthetic application logs and sends them to the log-ingestion-service.

## Run

pip install -r requirements.txt
python3 simulator.py
